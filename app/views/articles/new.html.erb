<%= form_for @article do |f| %>
	<div class="field">
		<%= f.label :title %>
		<%= f.text_field :title %>
	</div>
	
	<div class="field">
		<%= f.label :text %>
		<%= f.text_field :text %>
		<%= f.text_area :text %>
	</div>
	
	<%= f.submit "Submit", class: "btn btn-large btn-primary" %>

<% end %>
